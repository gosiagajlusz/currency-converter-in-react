import { ThemeProvider } from "styled-components";
import styled from "styled-components";

export const theme = {
  colors: {
    primaryColor: "#008080",
    navyBlue:  "#113850",
    coldGreen: "#10382a",
    lightMint: "#c4d7c7",
  },

  breakpoints: {
    mobileMax: 767,
  },
};
